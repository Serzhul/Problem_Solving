function solution(relation) {
    let answer = 0;

    const comb = (list, arr, idx, n, r, target) {
        if(r === 0) {
            list.push(Object.assign([], arr))
        } else if ()
    }
}

solution([
    ['100', 'ryan', 'music', '2'],
    ['200', 'apeach', 'math', '2'],
    ['300', 'tube', 'computer', '3'],
    ['400', 'con', 'computer', '4'],
    ['500', 'muzi', 'music', '3'],
    ['600', 'apeach', 'music', '2'],
]);
